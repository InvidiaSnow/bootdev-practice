[project]
name = "dts-fleet-overview"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Sølve", email = "solve.sem@leakdetector.no" }
]
requires-python = ">=3.14"
dependencies = []

[project.scripts]
bootdev-practice = "bootdev_practice:main"

[build-system]
requires = ["uv_build>=0.12.9,<0.13.0"]
build-backend = "uv_build"
