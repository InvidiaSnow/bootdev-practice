# Ixon Cloud credentials (required)
IXON_EMAIL=your@email.com

# InfluxDB API token (optional — the script will prompt if not set)
# Each device has its own token. Once entered, it's saved automatically.
# To get a token: InfluxDB sidebar -> Load Data -> API Tokens -> Generate API Token
API_TOKEN=
